/*
import styled from 'styled-components';

export default styled.div`
  display: flex;
  flex-direction: row;
  transform: translate(${(props) => -props.position}px, ${0}px);
  margin-left: (${(props) => -props.page * props.width});
  transition: (${(props) => (props.animated ? 'all 300ms 0ms' : '')});
`;
*/
